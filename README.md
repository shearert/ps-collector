Usage
1. Install the rsv-perfsonar RPM
2. Enable the local network probe
rsv-control --enable org.osg.local.network-monitoring-local --host myhost

Where myhost is the host where rsvpersonar is installed
